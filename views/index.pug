<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Articles
        link(rel="stylesheet", href="http://localhost:3000/bootstrap/dist/css/bootstrap.min.css")
    body
        h1 pug template

        div.article
            each article in articles
                div
                    span= article.title
                div.btn.btn-sm.btn-danger.m-2
                    a(href=`articles/delete/${article.id}`) Delete
                div.btn.btn-sm.btn-info.m-2
                    a(href=`articles/edit/${article.id}`) Edit
        div.btn.btn-danger
            a(href="articles/new") New Article